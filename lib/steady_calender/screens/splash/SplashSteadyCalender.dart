import 'package:flutter/cupertino.dart';

class SplashSteadyCalender extends StatelessWidget {
  const SplashSteadyCalender({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
